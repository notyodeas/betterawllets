import { YesnotspacesPipe } from './yesnotspaces.pipe';

describe('YesnotspacesPipe', () => {
  it('create an instance', () => {
    const pipe = new YesnotspacesPipe();
    expect(pipe).toBeTruthy();
  });
});
